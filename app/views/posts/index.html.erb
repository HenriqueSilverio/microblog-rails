<% @posts.each do |post| %>
  <article class="resume">
    <h1><%= link_to post.titulo, post, :class => 'resume__title' %></h1>
    <p><%= sanitize(truncate(post.conteudo, :length => 300)) %></p>
    <%= link_to 'Leia mais', post, :class => 'more' %>
    <!-- <%= link_to 'Editar', edit_post_path(post) %> -->
    <!-- <%= link_to 'Excluir', post, method: :delete, data: { confirm: 'Are you sure?' } %> -->
  </article>
<% end %>
